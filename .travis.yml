language: bash
services:
  - docker
script:
  - bash artifact-test2.sh
addons:
  artifacts:
    debug: true
    paths:
      - ${TRAVIS_BUILD_DIR}/artifacts
    target_paths:
      - /${TRAVIS_BUILD_NUMBER}/${TRAVIS_JOB_NUMBER}